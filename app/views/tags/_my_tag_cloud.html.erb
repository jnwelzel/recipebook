<h3 class="caps">My Tag cloud </h3>
<div class="box">
<% tag_cloud(current_user.recipes.tag_counts_on(:tags), %w(tag4 tag3 tag2 tag1)) do |tag, css_class| %>
  <%= link_to tag.name, tag_path(tag.name), :class => css_class %>
<% end %>
</div>

